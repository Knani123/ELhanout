import React from "react";
import "./style.css";

const PubHome = ({ image }) => {
  console.log(image);
  return (
    <div
      className="sous-bar"
      style={{
        height: "100px",
        // overflow: "hidden",
      }}
    >
      <h1></h1>
    </div>
  );
};

export default PubHome;
